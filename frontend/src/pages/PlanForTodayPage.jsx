/**
 * Plan for Today (M20 will build nearest-neighbor route + export).
 */
export default function PlanForTodayPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-slate-50">
      <div className="text-center">
        <h1 className="text-2xl font-bold mb-2">Plan for Today</h1>
        <p className="text-slate-500">Multi-select + nearest-neighbor routing arrives in M20.</p>
      </div>
    </div>
  );
}
