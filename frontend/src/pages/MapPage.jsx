/**
 * Map view (M18 will flesh out with Leaflet + Mapbox tiles).
 * M15 scaffolds a placeholder so routing works.
 */
export default function MapPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-slate-50">
      <div className="text-center">
        <h1 className="text-2xl font-bold mb-2">Map</h1>
        <p className="text-slate-500">Pins + clusters will render here in M18.</p>
      </div>
    </div>
  );
}
